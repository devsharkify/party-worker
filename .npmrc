# React Native / Expo resolve transitive deps by walking node_modules, which
# pnpm's isolated layout hides. shamefully-hoist hoists them to the root
# node_modules (Metro's resolver checks there) while keeping workspace package
# symlinks and bin resolution intact (which node-linker=hoisted broke).
shamefully-hoist=true
